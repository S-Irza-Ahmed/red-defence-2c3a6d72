/**
 * API / data-fetching layer
 *
 * Place all backend calls, REST/GraphQL clients, and data transformation
 * utilities in this folder. Group by domain (e.g. auth.ts, scan.ts).
 *
 * Currently the app uses mock data — when you integrate a real backend,
 * add service modules here and import them from feature pages.
 */
